apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: replenishment
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/tmlconnected/ep-pipelines.git
    targetRevision: argocd-test
    path: argocd/helm/app
    helm:
      releaseName: replenishment
      valueFiles:
        - "../../deploy/ipms4/dev/replenishment/values.yaml"
  destination:
    name: in-cluster
    namespace: sadhan
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  revisionHistoryLimit: 10
