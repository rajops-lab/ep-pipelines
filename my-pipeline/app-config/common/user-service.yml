# Harry's Generated App Config: user-service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: harry-dev
  labels:
    app: user-service
    environment: dev
    generated-by: harry
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
        environment: dev
    spec:
      containers:
      - name: user-service
        image: node:16
        ports:
        - containerPort: 3000
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"

---
apiVersion: v1
kind: Service
metadata:
  name: user-service-service
  namespace: harry-dev
spec:
  selector:
    app: user-service
  ports:
  - port: 3000
    targetPort: 3000
  type: ClusterIP
